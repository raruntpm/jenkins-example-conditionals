pipeline {
  agent any
  triggers { 
    cron('* * * * *') 
  }
  stages {
    stage('Example Deploy') {
      when {
        triggeredBy 'TimerTrigger'
      }
      steps {
        echo 'echo Deploying'
      }
    }
  }
}
